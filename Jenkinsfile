//Declerative pipeline
pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				echo "Build"
			}
		}
		stage('Build') {
			steps {
				echo "Test"
			}
		}
		stage('Build') {
			steps {
				echo "Integration Test"
			}
		}

	}
}
